const initialState = {
  pref: {
    company: [],
  },
}

const mainReducer = (state = initialState, action) => {
  switch (action.type) {
    // case:
    // case:
    default:
      return state
  }
}
export default mainReducer
